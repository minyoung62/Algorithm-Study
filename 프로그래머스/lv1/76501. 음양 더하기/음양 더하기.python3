def solution(absolutes, signs):
    answer = 0
    print(signs)
    for i in range(len(absolutes)):
    
        if signs[i]:
            answer += absolutes[i]
        else:
           
            answer -= absolutes[i]
     
    

    return answer